#!/bin/bash
# Check if the file 'hello' exists
if [ -e hello ]; then
    # Get the owner of the file
    owner=$(stat -c '%U' hello)
    
    # Change owner to vincent only if current owner is guillaume
    if [ "$owner" = "guillaume" ]; then
        sudo chown vincent hello
    fi
fi
